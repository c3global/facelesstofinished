<?xml version="1.0" encoding="utf-8"?><testsuites name="pytest tests"><testsuite name="pytest" errors="0" failures="4" skipped="0" tests="10" time="1.146" timestamp="2026-06-30T10:44:32.177416+00:00" hostname="agent-env-077efd7d-6947-4238-b6ab-81b7a4345f77"><testcase classname="backend.tests.test_iter36_byok_anthropic_and_lightbox.TestMeQuotaRegression" name="test_owner_quota_has_byok_allowed_true" time="0.320" /><testcase classname="backend.tests.test_iter36_byok_anthropic_and_lightbox.TestByokAnthropicServiceList" name="test_list_has_four_services_with_anthropic" time="0.102" /><testcase classname="backend.tests.test_iter36_byok_anthropic_and_lightbox.TestByokAnthropicCRUD" name="test_save_get_delete_anthropic_key" time="0.460" /><testcase classname="backend.tests.test_iter36_byok_anthropic_and_lightbox.TestByokAnthropicCRUD" name="test_short_anthropic_key_rejected" time="0.092" /><testcase classname="backend.tests.test_iter36_byok_anthropic_and_lightbox.TestByokAnthropicWiringStatic" name="test_claude_complete_has_user_email_and_byok" time="0.001"><failure message="AssertionError: _claude_complete signature must include user_email&#10;assert None">backend/tests/test_iter36_byok_anthropic_and_lightbox.py:172: in test_claude_complete_has_user_email_and_byok
    assert m, "_claude_complete signature must include user_email"
E   AssertionError: _claude_complete signature must include user_email
E   assert None</failure></testcase><testcase classname="backend.tests.test_iter36_byok_anthropic_and_lightbox.TestByokAnthropicWiringStatic" name="test_callers_pass_user_email" time="0.001"><failure message="AssertionError: angles caller must pass user_email=user.email&#10;assert None&#10; +  where None = &lt;function search at 0xff170e61d4e0&gt;('_claude_complete\\(\\s*ANGLES_SYSTEM_PROMPT[^)]*user_email=user\\.email', '&quot;&quot;&quot;F2F48 Studio backend — FastAPI.\n\nAuth model: Netlify (faceless48.c3global.co) is the source of truth for paying\ncustomers + entitlements. This backend either (a) forwards the user\'s cookies\nto the Netlify /api/auth-me endpoint and trusts its response, or (b) honors a\nDEV_BYPASS_EMAIL env var so the Studio UI is testable on the Emergent preview\nURL where cross-domain cookies aren\'t available.\n\nAfter a successful Netlify auth-me handshake we mint a short-lived JWT and\nstore it in MongoDB so subsequent calls don\'t re-hit Netlify on every request.\n&quot;&quot;&quot;\nfrom __future__ import annotations\n\nimport asyncio\nimport json\nimport logging\nimport os\nimport re\nimport secrets\nimport subprocess\nimport tempfile\nimport time\nimport uuid\nimport wave\nfrom datetime import datetime, timedelta, timezone\nfrom typing import Any, Optional\n\nimport fal_client\nimport httpx\nimport imageio_ffmpeg\nimport jwt\nfrom dotenv import load_dotenv\nfrom fastapi import Body, Cookie, Depends, FastAPI, Header, HTTPException, Path, Query, Request\nfrom fastapi.middleware.cors import CORSMiddleware\nfrom fastapi.responses import JSONResponse\nfrom motor.motor_asyncio import AsyncIOMotorClien...umo launch logic can evolve independently.\nfrom licenses_routes import register_license_routes  # noqa: E402\n\nregister_license_routes(\n    api=api,\n    db=db,\n    current_user_dep=current_user,\n    require_admin_dep=require_admin,\n    log_activity=_log_activity,\n    dev_bypass_email=DEV_BYPASS_EMAIL,\n    studio_grant_emails=STUDIO_GRANT_EMAILS,\n)\n\n# BYOK vault — encrypted Anthropic / OpenAI / HeyGen / fal.ai keys for\n# T4 / Founder users. (get_byok_key was imported above for the thumbnails\n# rewriter; register_byok_routes mounts /api/user/byok* here.)\n# The module also exposes get_byok_key(db, email, service) which the render\n# paths call to decide whether to use a customer\'s key vs the platform key.\n\nregister_byok_routes(\n    api=api,\n    db=db,\n    current_user_dep=current_user,\n    dev_bypass_email=DEV_BYPASS_EMAIL,\n    studio_grant_emails=STUDIO_GRANT_EMAILS,\n)\n\n\n# ---------------------------------------------------------------------------\n# Mount\n# ---------------------------------------------------------------------------\napp.mount(&quot;/api&quot;, api)\n\n\n@app.get(&quot;/&quot;)\nasync def root():\n    return {&quot;service&quot;: &quot;F2F48 Studio API&quot;, &quot;status&quot;: &quot;ok&quot;}\n', flags=re.DOTALL)&#10; +    where &lt;function search at 0xff170e61d4e0&gt; = re.search&#10; +    and   '&quot;&quot;&quot;F2F48 Studio backend — FastAPI.\n\nAuth model: Netlify (faceless48.c3global.co) is the source of truth for paying\ncustomers + entitlements. This backend either (a) forwards the user\'s cookies\nto the Netlify /api/auth-me endpoint and trusts its response, or (b) honors a\nDEV_BYPASS_EMAIL env var so the Studio UI is testable on the Emergent preview\nURL where cross-domain cookies aren\'t available.\n\nAfter a successful Netlify auth-me handshake we mint a short-lived JWT and\nstore it in MongoDB so subsequent calls don\'t re-hit Netlify on every request.\n&quot;&quot;&quot;\nfrom __future__ import annotations\n\nimport asyncio\nimport json\nimport logging\nimport os\nimport re\nimport secrets\nimport subprocess\nimport tempfile\nimport time\nimport uuid\nimport wave\nfrom datetime import datetime, timedelta, timezone\nfrom typing import Any, Optional\n\nimport fal_client\nimport httpx\nimport imageio_ffmpeg\nimport jwt\nfrom dotenv import load_dotenv\nfrom fastapi import Body, Cookie, Depends, FastAPI, Header, HTTPException, Path, Query, Request\nfrom fastapi.middleware.cors import CORSMiddleware\nfrom fastapi.responses import JSONResponse\nfrom motor.motor_asyncio import AsyncIOMotorClien...umo launch logic can evolve independently.\nfrom licenses_routes import register_license_routes  # noqa: E402\n\nregister_license_routes(\n    api=api,\n    db=db,\n    current_user_dep=current_user,\n    require_admin_dep=require_admin,\n    log_activity=_log_activity,\n    dev_bypass_email=DEV_BYPASS_EMAIL,\n    studio_grant_emails=STUDIO_GRANT_EMAILS,\n)\n\n# BYOK vault — encrypted Anthropic / OpenAI / HeyGen / fal.ai keys for\n# T4 / Founder users. (get_byok_key was imported above for the thumbnails\n# rewriter; register_byok_routes mounts /api/user/byok* here.)\n# The module also exposes get_byok_key(db, email, service) which the render\n# paths call to decide whether to use a customer\'s key vs the platform key.\n\nregister_byok_routes(\n    api=api,\n    db=db,\n    current_user_dep=current_user,\n    dev_bypass_email=DEV_BYPASS_EMAIL,\n    studio_grant_emails=STUDIO_GRANT_EMAILS,\n)\n\n\n# ---------------------------------------------------------------------------\n# Mount\n# ---------------------------------------------------------------------------\napp.mount(&quot;/api&quot;, api)\n\n\n@app.get(&quot;/&quot;)\nasync def root():\n    return {&quot;service&quot;: &quot;F2F48 Studio API&quot;, &quot;status&quot;: &quot;ok&quot;}\n' = &lt;test_iter36_byok_anthropic_and_lightbox.TestByokAnthropicWiringStatic object at 0xff170b212d50&gt;.server_src&#10; +    and   re.DOTALL = re.DOTALL">backend/tests/test_iter36_byok_anthropic_and_lightbox.py:184: in test_callers_pass_user_email
    assert re.search(
E   AssertionError: angles caller must pass user_email=user.email
E   assert None
E    +  where None = &lt;function search at 0xff170e61d4e0&gt;('_claude_complete\\(\\s*ANGLES_SYSTEM_PROMPT[^)]*user_email=user\\.email', '"""F2F48 Studio backend — FastAPI.\n\nAuth model: Netlify (faceless48.c3global.co) is the source of truth for paying\ncustomers + entitlements. This backend either (a) forwards the user\'s cookies\nto the Netlify /api/auth-me endpoint and trusts its response, or (b) honors a\nDEV_BYPASS_EMAIL env var so the Studio UI is testable on the Emergent preview\nURL where cross-domain cookies aren\'t available.\n\nAfter a successful Netlify auth-me handshake we mint a short-lived JWT and\nstore it in MongoDB so subsequent calls don\'t re-hit Netlify on every request.\n"""\nfrom __future__ import annotations\n\nimport asyncio\nimport json\nimport logging\nimport os\nimport re\nimport secrets\nimport subprocess\nimport tempfile\nimport time\nimport uuid\nimport wave\nfrom datetime import datetime, timedelta, timezone\nfrom typing import Any, Optional\n\nimport fal_client\nimport httpx\nimport imageio_ffmpeg\nimport jwt\nfrom dotenv import load_dotenv\nfrom fastapi import Body, Cookie, Depends, FastAPI, Header, HTTPException, Path, Query, Request\nfrom fastapi.middleware.cors import CORSMiddleware\nfrom fastapi.responses import JSONResponse\nfrom motor.motor_asyncio import AsyncIOMotorClien...umo launch logic can evolve independently.\nfrom licenses_routes import register_license_routes  # noqa: E402\n\nregister_license_routes(\n    api=api,\n    db=db,\n    current_user_dep=current_user,\n    require_admin_dep=require_admin,\n    log_activity=_log_activity,\n    dev_bypass_email=DEV_BYPASS_EMAIL,\n    studio_grant_emails=STUDIO_GRANT_EMAILS,\n)\n\n# BYOK vault — encrypted Anthropic / OpenAI / HeyGen / fal.ai keys for\n# T4 / Founder users. (get_byok_key was imported above for the thumbnails\n# rewriter; register_byok_routes mounts /api/user/byok* here.)\n# The module also exposes get_byok_key(db, email, service) which the render\n# paths call to decide whether to use a customer\'s key vs the platform key.\n\nregister_byok_routes(\n    api=api,\n    db=db,\n    current_user_dep=current_user,\n    dev_bypass_email=DEV_BYPASS_EMAIL,\n    studio_grant_emails=STUDIO_GRANT_EMAILS,\n)\n\n\n# ---------------------------------------------------------------------------\n# Mount\n# ---------------------------------------------------------------------------\napp.mount("/api", api)\n\n\n@app.get("/")\nasync def root():\n    return {"service": "F2F48 Studio API", "status": "ok"}\n', flags=re.DOTALL)
E    +    where &lt;function search at 0xff170e61d4e0&gt; = re.search
E    +    and   '"""F2F48 Studio backend — FastAPI.\n\nAuth model: Netlify (faceless48.c3global.co) is the source of truth for paying\ncustomers + entitlements. This backend either (a) forwards the user\'s cookies\nto the Netlify /api/auth-me endpoint and trusts its response, or (b) honors a\nDEV_BYPASS_EMAIL env var so the Studio UI is testable on the Emergent preview\nURL where cross-domain cookies aren\'t available.\n\nAfter a successful Netlify auth-me handshake we mint a short-lived JWT and\nstore it in MongoDB so subsequent calls don\'t re-hit Netlify on every request.\n"""\nfrom __future__ import annotations\n\nimport asyncio\nimport json\nimport logging\nimport os\nimport re\nimport secrets\nimport subprocess\nimport tempfile\nimport time\nimport uuid\nimport wave\nfrom datetime import datetime, timedelta, timezone\nfrom typing import Any, Optional\n\nimport fal_client\nimport httpx\nimport imageio_ffmpeg\nimport jwt\nfrom dotenv import load_dotenv\nfrom fastapi import Body, Cookie, Depends, FastAPI, Header, HTTPException, Path, Query, Request\nfrom fastapi.middleware.cors import CORSMiddleware\nfrom fastapi.responses import JSONResponse\nfrom motor.motor_asyncio import AsyncIOMotorClien...umo launch logic can evolve independently.\nfrom licenses_routes import register_license_routes  # noqa: E402\n\nregister_license_routes(\n    api=api,\n    db=db,\n    current_user_dep=current_user,\n    require_admin_dep=require_admin,\n    log_activity=_log_activity,\n    dev_bypass_email=DEV_BYPASS_EMAIL,\n    studio_grant_emails=STUDIO_GRANT_EMAILS,\n)\n\n# BYOK vault — encrypted Anthropic / OpenAI / HeyGen / fal.ai keys for\n# T4 / Founder users. (get_byok_key was imported above for the thumbnails\n# rewriter; register_byok_routes mounts /api/user/byok* here.)\n# The module also exposes get_byok_key(db, email, service) which the render\n# paths call to decide whether to use a customer\'s key vs the platform key.\n\nregister_byok_routes(\n    api=api,\n    db=db,\n    current_user_dep=current_user,\n    dev_bypass_email=DEV_BYPASS_EMAIL,\n    studio_grant_emails=STUDIO_GRANT_EMAILS,\n)\n\n\n# ---------------------------------------------------------------------------\n# Mount\n# ---------------------------------------------------------------------------\napp.mount("/api", api)\n\n\n@app.get("/")\nasync def root():\n    return {"service": "F2F48 Studio API", "status": "ok"}\n' = &lt;test_iter36_byok_anthropic_and_lightbox.TestByokAnthropicWiringStatic object at 0xff170b212d50&gt;.server_src
E    +    and   re.DOTALL = re.DOTALL</failure></testcase><testcase classname="backend.tests.test_iter36_byok_anthropic_and_lightbox.TestByokAnthropicWiringStatic" name="test_run_script_job_has_user_email_and_byok" time="0.001" /><testcase classname="backend.tests.test_iter36_byok_anthropic_and_lightbox.TestByokAnthropicWiringStatic" name="test_thumbnails_rewrite_prompt_byok_branch" time="0.000"><failure message="AssertionError: rewrite-prompt route not found&#10;assert None">backend/tests/test_iter36_byok_anthropic_and_lightbox.py:223: in test_thumbnails_rewrite_prompt_byok_branch
    assert m, "rewrite-prompt route not found"
E   AssertionError: rewrite-prompt route not found
E   assert None</failure></testcase><testcase classname="backend.tests.test_iter36_byok_anthropic_and_lightbox.TestByokAnthropicWiringStatic" name="test_thumbnails_concepts_from_script_byok_branch" time="0.000"><failure message="AssertionError: concepts-from-script route not found&#10;assert None">backend/tests/test_iter36_byok_anthropic_and_lightbox.py:237: in test_thumbnails_concepts_from_script_byok_branch
    assert m, "concepts-from-script route not found"
E   AssertionError: concepts-from-script route not found
E   assert None</failure></testcase><testcase classname="backend.tests.test_iter36_byok_anthropic_and_lightbox.TestByokAnthropicWiringStatic" name="test_byok_services_dict" time="0.001" /></testsuite></testsuites>