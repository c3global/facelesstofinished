<analysis>**original_problem_statement**: Moving F2F48 (Script Engine + Studio) from Netlify to emergent.sh. Rebuild the Studio UI to match the modal-chip pattern, improve render pipelines, and natively host an Admin Panel gated by JWT. The product has pivoted to a 27/mo community membership model. 
PRODUCT REQUIREMENTS: 
1. Retain working features: Script Engine, Studio, Auth, Admin Panel.
2. Rebuild Studio UI with Modal-chip patterns.
3. Improve Render Pipelines (parallel execution, local FFmpeg compose, graceful fallbacks).
4. Implement new Tier System (Starter, Legacy, Founder, Premium).
5. BYOK (Bring Your Own Key) enabled for all users.

**User's preferred language**: English (INSTRUCT next agent to respond in English only)

**what currently exists?** 
A React + FastAPI + MongoDB app. It features a Script Engine, Studio UI, Admin Panel, Timeline Editor MVP, and Magic Link auth with admin bypass. The tier system has been fully migrated to support the new business model. The rendering engine relies on an async background worker in FastAPI. The composition step now utilizes local FFmpeg processing (with a 3-tier fallback system) to stitch audio, stock/AI video, and captions, dramatically reducing reliance on external fal.ai storage.

**Last working item**: 
    - Last item agent was working: Fixing the local FFmpeg compose fallback logic (v1.20.7). The fast-path stream-copy () failed when stock clips had varying H.264 profiles. The agent implemented a 3-tier fallback: (1) Stream-copy, (2) Re-encode locally, (3) Upload to fal.ai for remote composition.
    - Status: USER VERIFICATION PENDING
    - Agent Testing Done: Y
    - Which testing method agent to use?: backend testing agent/ curl / python -c
    - User Testing Done: N (User needs to redeploy to production to see the fix).

**All Pending/In progress Issue list**:  
  Issue 1: Long-format renders (15-25 min) look repetitive and boring [P0]
     - Attempted fixes: Agent analyzed the math and proposed a solution but user explicitly said Do not touch any code for now.
     - Next debug checklist: 1. Implement Length-adaptive scene caps in  ( should change from fixed 12 to 20 for medium, and 40 for long). 2. Implement Auto-cutaways for scenes lasting > 10s.
     - Why fix this issue and what will be achieved with the fix?: Currently, a 25-minute video only generates 12 scenes (average 2 minutes per stock clip), which is terrible UX. 
     - Status: NOT STARTED (Awaiting user go signal).
     - Is recurring issue? N
     - Should Test frontend/backend/both after fix?: Backend
     - Blocked on other issue: User approval

  Issue 2: Timeline Editor is only available post-render [P1]
     - Attempted fixes: None yet.
     - Next debug checklist: Rethink the UX in  so users can edit the timeline/beats *before* the heavy render composition phase executes.
     - Why fix this issue and what will be achieved with the fix?: User explicitly complained: I dont like that it is only available in post."
     - Status: NOT STARTED
     - Is recurring issue? N
     - Should Test frontend/backend/both after fix?: Both
     - Blocked on other issue: None

  Issue 3: Phase 2 Provider Abstraction Refactor in `server.py` [P2]
     - Attempted fixes: Deferred to handle urgent pipeline stability bugs.
     - Next debug checklist: Refactor `server.py` to build abstract provider classes (`ImageProvider`, `VideoMotionProvider`, `VoiceProvider`, `StockProvider`, `RenderCompositionProvider`). 
     - Why fix this issue and what will be achieved with the fix?: `server.py` is over 5,600 lines long. Refactoring deeply uncouples the app from monolithic dependencies.
     - Status: NOT STARTED
     - Is recurring issue? N
     - Should Test frontend/backend/both after fix?: Both (Requires rigorous backend testing agent use)
     - Blocked on other issue: None

**In progress Task List**: None.

**Upcoming and Future Tasks**
    Upcoming Tasks:
    - [P1] Timeline Editor v2: Add drag sliders, waveform visualization, and per-sentence TTS timing.
    - [P2] Build a Demo Sales Page at `/` (Homepage for non-logged-in users).
    
    Future Tasks:
    - [P2] Canva Integration for B-Roll import (Paused due to API approval logistics).
    - [P2] Brand Voice Profiles.
    - [P2] Content Series Builder.

**Completed work in this session**
- Addressed 55% stuck render memory issue: Added Startup Reaper for orphaned renders, Semaphore concurrency cap for scene generation, and tuned FFmpeg memory usage. (DONE)
- Added "Cancel Render" backend endpoint and frontend UI button. (DONE)
- Implemented Tier Pivot & BYOK for all: Renamed tiers to Starter, Legacy, Founder, and Premium. Wrote migration script, updated UI/CSS. (DONE)
- Replaced fal.ai compose dependency with local FFmpeg composition (reduced render time by 21s and cut costs). (DONE)
- Built robust 3-tier fallback for local compose to handle mismatched bitstreams (Copy -> Re-encode -> Remote fal). (DONE)

**Earlier issues found/mentioned but not fixed**
None.

**Known issue recurrence from previous fork**
  - Issue recurrence in previous fork: Renders failing/hanging (55% or at compose step).
  - Recurrence count: 3+
  - Status: RESOLVED (Handled via concurrency caps, watchdog reaper, and shifting composition to local FFmpeg with robust fallbacks).
  Note: Be highly cautious modifying the render pipeline in `server.py` as it is extremely brittle regarding memory constraints and external API timeouts.

**Code Architecture**
```
/app/
├── backend/
│   ├── server.py (Main routes, heavy render orchestration, FFmpeg processing - >5.6k lines)
│   ├── tier_config.py (Rewritten for new business model)
│   ├── caption_burn_in.py
│   ├── prompts.py
│   └── tools/
│       └── migrate_tiers.py
├── frontend/
│   ├── src/
│   │   ├── pages/ (Login, Studio, SettingsKeys, etc.)
│   │   ├── components/ (TimelineModal.jsx, Admin tabs)
│   │   └── App.js, App.css
```

**Key Technical Concepts**
- React frontend + FastAPI backend + MongoDB.
- Async render pipeline with heavy background processing.
- FFmpeg local processing (`-c:v copy` and re-encoding).
- Resend Magic Link Auth with Admin Override.

**key DB schema**
  - `buyers`: {email, tier, entitlements, byok_allowed, founders, status}
  - `renders` / `jobs`: {id, status, progress, progress_label, error, local_video, cancel_requested}

**changes in tech stack**
  - Fal.ai is no longer the primary video composition engine. Local FFmpeg is now the primary engine, with Fal.ai acting strictly as a tier-3 fallback.

**All files of reference** 
  - `/app/backend/server.py` (Heavily modified for Reaper, Concurrency limits, FFmpeg local compose, and fallbacks).
  - `/app/backend/tier_config.py` (Rewritten to define Starter, Legacy, Founder, Premium tiers).
  - `/app/backend/tools/migrate_tiers.py` (Created for one-shot DB migration).
  - `/app/frontend/src/pages/Studio.jsx` (Added Cancel button, made Timeline button more prominent).
  - `/app/frontend/src/App.css` (Updated ent-chip colors).
  - `/app/frontend/src/components/admin/LicensesTab.jsx` & `UsageTab.jsx` (Removed AppSumo bias).

**Areas that need refactoring**
  - `/app/backend/server.py` needs modularization (Phase 2 Provider Abstraction). It is far too large and handles too much orchestration.

**key api endpoints**
  - `POST /studio/render/{job_id}/cancel` (New Cancel endpoint)
  - `POST /studio/render`
  - `GET /user/byok`

**Critical Info for New Agent** 
- **ENVIRONMENT CONFUSION:** The user strictly tests on their **PRODUCTION** deployment (`faceless48.c3global.co`), but you only have access to the **PREVIEW** environment. When the user complains that "its still broken, it is often because they haven't redeployed the fixes you just merged in preview. Fix the code in preview and politely wait for the user to test (do not lecture them on deployment ops).
- **Video Logic:** The render pipeline is highly sensitive. The transition to local FFmpeg was done to save fal.ai API timeouts. Do not revert to uploading intermediate clips to Fal.ai unless it is the final fallback.
- **Tiers:** Starter (Base), Legacy (Base+Shorts), Founder (Base+Shorts+Studio lifetime), Premium (Base+Shorts+Studio 27/mo). BYOK is allowed for ALL tiers.

**documents and test reports created in this job**
  -  
  -  
  - 

**Last 10 User Messages and any pending HUMAN messages** 
1. (Latest) User stated that the local FFmpeg compose failed on their end (in prod) and the fallback didn't work. (Agent fixed the bitstream mismatch bug with a 3-tier fallback).
2. User asked how long a 25-minute video takes and complained that 12 scenes is too few for long videos. Demanded agent do not touch any code.
3. User rejected UI enhancements (Premium checkout/Landing pages) and demanded focus solely on render stability.
4. User corrected agent on Tier structure: Starter, Legacy, Founder, Premium. Approved migration.
5. User expressed extreme frustration that the timeline editor is only available post-render.
6. User complained about a 75-second timeout error for a specific client (Volker) in Germany.
7. User authorized tier rename and enabling BYOK for everyone.
8. User complained that the app was stuck at 55% memory and an AI bot told them to upgrade server RAM.
9. User confirmed Reaper timeouts (5m) and concurrency caps (3).
10. User handed off session.

**Project Health Check:**
- Broken: UX for Timeline Editor (User hates it being post-render). Long-render visual pacing (capped at 12 scenes).
- Mocked: None.

**3rd Party Integrations** 
- Anthropic Claude (Emergent LLM Key)
- Gemini Nano Banana (Emergent LLM Key)
- OpenAI (Emergent LLM Key)
- HeyGen (User API Key)
- ElevenLabs (User API Key)
- fal.ai (User API Key - now used primarily for storage/fallback)
- Kokoro TTS

**Testing status**
  - Testing agent used after significant changes: NO (Heavy reliance on manual bash Python scripts to simulate FastAPI background tasks to avoid context bloat).
  - Troubleshoot agent used after agent stuck in loop: NO
  - Test files created: ,  (Deleted after testing).
  - Known regressions: None currently known in preview.

**Credentials to test flow:**
Available in .

**What agent forgot to execute**
The agent addressed the long video scene cap conceptually (suggesting 40 scenes + auto cutaways) but did not implement it because the user explicitly ordered Do not touch any code while they were querying render metrics. This remains a high-priority UX fix to pick up once the user approves.</analysis>
